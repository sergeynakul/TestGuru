en:
  activerecord:
    model:
      test: "Test"

    attributes:
      test:
        title: "Title"
        category: "Category"

    errors:
      messages:
        blank: "can't be blank"
        required: "can't be blank"
